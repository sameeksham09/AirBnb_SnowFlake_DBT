snapshots:
    - name: dim_hosts
      relation : ref('hosts')
      config:
            schema : gold
            database : AIRBNB
            unique_key : host_id
            strategy : check
            check_cols : all
            db_valid_to_current : "to_date('9999-12-31')"
