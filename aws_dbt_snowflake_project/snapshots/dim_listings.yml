snapshots:
    - name: dim_listings
      relation : ref('listings')
      config:
            schema : gold
            database : AIRBNB
            unique_key : listing_id
            strategy : check
            check_cols : all
            db_valid_to_current : "to_date('9999-12-31')"
